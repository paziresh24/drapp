import Modal from '../../core/modal';

const index = () => {
    return (
        <Modal title="کد تایید">
            <span>کد تایید ارسال شده را وارد نمایید.</span>
        </Modal>
    );
};

export default index;
